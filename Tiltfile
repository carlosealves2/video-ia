# Video IA Monorepo Tiltfile

# Load apps
include('./packages/service-discover/Tiltfile')

# Add more apps here as they are created:
# include('./packages/api/Tiltfile')
# include('./packages/worker/Tiltfile')
